name: Deploy Docs

on:
  push:
    branches: [master]
    paths:
      - 'wiki/**'
      - '.github/workflows/docs.yml'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: wiki/package-lock.json

      - name: Install dependencies
        working-directory: wiki
        run: npm ci

      - name: Restore Docusaurus cache
        uses: actions/cache@v4
        with:
          path: wiki/node_modules/.cache
          key: docusaurus-${{ hashFiles('wiki/package-lock.json') }}
          restore-keys: docusaurus-

      - name: Build docs (English)
        working-directory: wiki
        run: npx docusaurus build --locale en

      - name: Build docs (Chinese)
        working-directory: wiki
        run: npx docusaurus build --locale zh-CN

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: wiki/build

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
